Design a clean, minimal UI flow for a dental clinical documentation web and mobile app. The style should be calm, professional, and medical — similar to Ojas or Linear. Use a warm off-white background (#F7F5F0), dark ink text (#1A1814), and a muted green accent (#2D6A4F). Font should be clean sans-serif. No loud colors. No gradients. Show both a desktop web layout and a mobile layout side by side where relevant, or just show mobile screens in a horizontal flow connected by arrows for the core flow. All screens should feel consistent across both form factors.
Show these screens in a horizontal flow connected by arrows:
Screen 1 — Login
Centered card on a warm off-white background. App logo and name at top — "DentAI" in serif font with a small green dot. Below that, a "Welcome back, Doctor" heading. Username field and password field below it. A green "Sign In" button. Nothing else. Clean and minimal.
Screen 2 — Home / Landing
Top bar shows doctor's name on the left and a small avatar or initials on the right. Below that a warm greeting — "Good morning, Dr. Priya". Below the greeting, two large tappable cards stacked vertically. First card: "New Session" with a microphone icon. Second card: "Patient History" with a folder icon. Below the two cards, a third section — a global AI query bar that says "Ask anything about your patients..." with a small sparkle or AI icon on the left. This is always visible on the home screen.
Screen 3 — Global AI Query (expanded)
Same home screen but the query bar is now expanded into a small chat-like panel anchored to the bottom. Doctor has typed "What did I prescribe Vikram last time?" and below it the AI has responded with a clean one-paragraph answer citing the session date as the source. Below the response, a small link that says "Open Vikram's record →". The panel can be dismissed. No full-screen takeover — it stays as a floating panel.
Screen 4 — New Session: Patient Search
Search bar at top that says "Search by name or number". Below it a button that says "+ New Patient". Below that, 3 recent patient rows each showing avatar initials, name, age, gender, and last visit date. Clean list style, subtle dividers.
Screen 5 — Active Session
Top shows patient name and a small "Last visit" date in muted text. Below that three action buttons in a row: red pulsing record button labeled "Record", an upload icon labeled "Upload", and a notes icon labeled "Note". Below the buttons, a live transcript area showing partial text appearing as if being typed in real time with a blinking cursor. Transcript text is in monospace light font. Bottom has a "Save & Close" button in green.
Screen 6 — Patient History Tab
Top has a search bar that says "Search by name or number". Below it a toggle or tab switch between "Search" and "All Patients". In All Patients view, patients are listed sorted by latest visit timestamp newest first. Each row shows patient initials avatar, patient name, last visit date, and 2-3 small colored tags like "RCT", "Follow-up", "Antibiotic". Subtle list style, no heavy borders.
Screen 7 — Patient Detail View
Patient name and basic info at top — age, gender, phone number in muted text. Below that a short AI-generated summary in a lightly shaded green-tinted card — 3 lines of placeholder summary text with a small sparkle icon indicating it is AI generated. Below the summary card, a patient-level AI query bar that says "Ask about this patient..." — this is scoped only to this patient's history. Below that, a timeline of sessions listed newest first. Each session row shows date and small document type tags — Transcript, Prescription, Note. Each row has a right arrow indicating tappable.
Screen 8 — Patient AI Query (expanded)
Same Patient Detail View but the patient-level query bar is expanded. Doctor has typed "Did this patient mention any allergies?" and the AI has responded citing the specific session date where allergies were mentioned. Source citation shown in muted text below the answer — "From session 12 Mar 2026". Dismissable panel, does not replace the screen.
Screen 9 — Session Detail View
Shows one expanded session. Date at top. Below that three collapsible sections stacked vertically. First section: "Raw Transcript" showing 4-5 lines of placeholder monospace text with an edit pencil icon — fully editable. Second section: "Structured Note" showing labeled fields — Chief Complaint, Diagnosis, Treatment Plan, Medications — each with placeholder values and inline edit icons. Third section: "Prescription" showing a thumbnail image on the left and extracted structured text fields on the right — Medication name, dose, frequency, duration. All sections editable inline. No read-only states anywhere.
Connect all screens with directional arrows. Label each arrow with the action that triggers the transition. Examples: "Sign In", "Tap New Session", "Tap query bar", "Dismiss panel", "Select Patient", "Tap Save & Close", "Tap History Tab", "Tap Patient Row", "Tap Ask about this patient", "Tap Session Row".
Also show a simple desktop web version of Screen 2 (Home) and Screen 7 (Patient Detail) as wider layouts where the left side is a sidebar with navigation and patient list, and the right side is the main content area. Same colors and style.